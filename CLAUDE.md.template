# CLAUDE.md — [PROJECT_NAME]

This file provides guidance to Claude Code when working with code in this repository.
Generated by `/bootstrap` from `PROJECT.md`. Update this file as the project evolves.

## Project Overview

[PROJECT_VISION]

## Architecture

- **Backend**: [BACKEND_TECH]
- **Frontend**: [FRONTEND_TECH]
- **Database**: [DATABASE_TECH]
- **Background Jobs**: [JOBS_TECH]
- **Deployment**: [DEPLOY_TECH]

## Key Technologies

[List key libraries, frameworks, and tools in use]

## Development Commands

```bash
# Start all services
[BUILD_CMD]

# Run tests
[TEST_CMD]

# Lint and format
[LINT_CMD]

# Database migrations
[MIGRATE_CMD]
```

## Project Structure

```
[PROJECT_NAME]/
├── [SOURCE_DIR]/          # Main source code
├── [TEST_DIR]/            # Tests
├── [DOCS_DIR]/            # Documentation
├── agents/                # Multi-agent workflow system
│   ├── requirements/      # REQ-NNNN.md — feature requirements
│   ├── architecture/      # ARCH-NNN.md — design documents
│   ├── handoffs/          # Inter-agent handoffs
│   ├── reviews/           # Tech Lead code reviews
│   ├── session-logs/      # Agent session logs
│   ├── templates/         # Document templates
│   └── workflow/          # Agent workflow definitions
├── PROJECT_STATUS.md      # Sprint status and agent assignments
├── PROJECT.md             # Project objectives (human input)
└── CLAUDE.md              # This file
```

## Multi-Agent Workflow

This project uses a multi-agent workflow defined in `agents/workflow/agents.md`.
Key commands:
- `/bootstrap` — Initialize project from PROJECT.md (run once)
- `/go` — Run sprint execution loop (activate agents for current sprint)
- `/plan [topic]` — Planning and feasibility session

## Testing

```bash
# All tests
[TEST_CMD]

# Specific test areas
[TEST_SPECIFIC_CMDS]
```

## Code Quality

Before committing:
```bash
[LINT_CMD]
[TEST_CMD]
```

## API Structure

[Describe the main API surface — REST endpoints, CLI interface, function signatures, etc.]

## Data Models

[List the core data entities and their relationships]

## Key Workflows

[Describe the most important end-to-end workflows in the system]

## Security Considerations

[List security requirements and constraints from PROJECT.md]

## Development Workflow

1. Check `agents/todo.md` for your assigned tasks
2. Read relevant REQ + ARCH documents before implementing
3. Write tests alongside implementation
4. Run `[LINT_CMD]` before committing
5. Use correct commit prefix (see `agents/workflow/agents.md`)
6. Create handoff for next agent after completing work

## Important Notes

[Any project-specific notes the AI needs to know — environment requirements,
external API dependencies, known issues, etc.]
